# In development, this should point to your backend API
VITE_API_URL=http://localhost:5001/api

# In production, when the client is served from the same domain as the API,
# you can leave this unset or set it to /api
# VITE_API_URL=/api
